#include"main.h"
/**
 * _isdigit - func to check the digit
 * @c: value to be checked
 * Return: will be 1 if itin the range
 */
int _isdigit(int c)
{
if ((c >= 48) && (c <= 57))
return (1);
else
return (0);

}
